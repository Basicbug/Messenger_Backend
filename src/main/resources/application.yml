server:
    port: 8080

spring:
    http:
        encoding:
            charset: UTF-8
            enabled: true
            force: true
    jpa:
        database: mysql
        database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
        hibernate:
            ddl-auto: create
        show-sql: true
        generate-ddl: true